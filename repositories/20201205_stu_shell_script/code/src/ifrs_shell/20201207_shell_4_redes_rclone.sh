# rclone | manage files on cloud

# esse pacote deve ser baixado, extensão .deb

# man dpkg : manager for debian packages

# sudo dpkg -i <file_path>.deb

## é possivel escolher diferentes tipos de cloud storages (ex: google drive)